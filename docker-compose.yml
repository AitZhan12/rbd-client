services:
  scraper:
    build: .
    container_name: scraper_rbd
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    extra_hosts:
      - "host.docker.internal:host-gateway"
    profiles: ["manual"]